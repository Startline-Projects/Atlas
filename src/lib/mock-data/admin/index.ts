/**
 * Barrel export for admin mock data. Future sessions:
 *
 *   import { PAGE_HEADER, ALERTS, QUICK_ACTIONS } from "@/lib/mock-data/admin";
 *
 * When real data wires up, replace each named export with a typed
 * fetcher (Server Component) or hook (Client) — the public shape stays
 * the same so call sites don't change.
 */

export * from "./dashboard-data";
export * from "./sidebar-nav-data";
export * from "./topbar-data";
