import { ComingSoon } from "@/components/specialist/shell/coming-soon";

// TODO: implemented in Session 4. Spec PDF Part 3 Step 6 (candidate chat) +
// Part 4 Step 8 (client chat). The route receives `?candidate=<id>` or
// `?client=<id>` from the my-candidates / my-clients sheets and from
// candidate-profile action buttons; until Session 4, this stub catches all
// /specialist/messages traffic.
export default function MessagesPage() {
  return <ComingSoon title="Messages" plannedSession={4} />;
}
