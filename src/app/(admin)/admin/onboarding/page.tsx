import { ObOnboardingTrigger } from '@/components/admin/onboarding/ob-onboarding-trigger';

export const metadata = {
  title: 'Onboarding tour',
};

export default function OnboardingPage() {
  return <ObOnboardingTrigger />;
}
