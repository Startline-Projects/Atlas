import { RtRealtimeShell } from '@/components/admin/realtime/rt-realtime-shell';

export const metadata = {
  title: 'Real-time updates',
};

export default function RealtimePage() {
  return <RtRealtimeShell />;
}
