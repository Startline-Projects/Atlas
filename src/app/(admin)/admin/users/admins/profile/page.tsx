import { AdminsShell } from '@/components/admin/users/admins/admins-shell';

export const metadata = {
  title: 'Admin accounts - Atlas',
  description: 'Admin profile detail with roster of all admins.',
};

export default function UsersAdminsProfilePage() {
  return <AdminsShell />;
}
