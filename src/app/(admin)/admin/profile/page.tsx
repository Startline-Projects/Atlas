import ProfileShell from '@/components/admin/profile/profile-shell';

export const metadata = {
  title: 'My Profile - Atlas',
  description: 'Admin profile, permissions, and account settings',
};

export default function AdminProfilePage() {
  return <ProfileShell />;
}
