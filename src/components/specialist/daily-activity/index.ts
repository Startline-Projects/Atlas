/**
 * Barrel for daily-activity-view-specific components. The page imports
 * `DailyActivityPage` only; the rest are internal to the view.
 */
export { DailyActivityPage } from "./daily-activity-page";
